# =============================================================================
# AetherLink Satellite Catalog Backend - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
SATCAT_HOST=0.0.0.0
SATCAT_PORT=9001
NODE_ENV=development

# -----------------------------------------------------------------------------
# PostgreSQL Database
# -----------------------------------------------------------------------------
# Format: postgres://user:password@host:port/database
POSTGRES_URL=postgres://satcat_user:your_password_here@localhost:5432/satcat_db

# -----------------------------------------------------------------------------
# Space-Track.org Credentials
# -----------------------------------------------------------------------------
# Register for free account at: https://www.space-track.org/auth/createAccount
SPACETRACK_USERNAME=your_email@example.com
SPACETRACK_PASSWORD=your_password_here

# Space-Track Rate Limiting (milliseconds between requests)
# Default: 1200ms (respects terms of use - max ~50 requests per minute)
SPACETRACK_MIN_INTERVAL_MS=1200

# -----------------------------------------------------------------------------
# SatNOGS DB
# -----------------------------------------------------------------------------
# No credentials required - SatNOGS DB is a public API

# -----------------------------------------------------------------------------
# Data Ingest Configuration
# -----------------------------------------------------------------------------
# How many days back to fetch during delta/incremental updates
INGEST_DAYS_BACK=7

# Maximum records to fetch per query (for testing)
# Remove or set to 0 for unlimited
# INGEST_LIMIT=1000
